const createButtonsWrapper = function(){
    const wrapper = document.createElement('div');
    wrapper.classList.add('project-details-buttons-wrapper')
    
    return wrapper;
}

export default createButtonsWrapper;